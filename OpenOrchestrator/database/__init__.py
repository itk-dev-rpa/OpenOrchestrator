"""Database layer: SQLAlchemy ORM models and query helpers.

Exposes the ORM classes for triggers, queues, jobs, logs, schedulers
and constants/credentials, along with the access functions in
``db_util`` (split per domain into ``trigger_util``, ``queue_util``,
``settings_util`` and ``log_util``).
"""
